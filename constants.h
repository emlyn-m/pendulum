#pragma once

namespace constants {
    inline float gravity = 9.81f;
    inline float ballMass = .2f;
    inline float stringLength = .2;
    inline int numBalls = 3;
    
    inline float rootPosition[] = {0, .5f};
    
    inline float pendulumMspt = 1000.f;
    
    inline float drawCircleSize = .1f;
    inline float drawStringLength = .1f;
    
    inline float PI = 3.14159265f;
}